services:
  youtube-zero:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - yt_data:/data
    environment:
      DB_PATH: /data/youtube_zero.db
      YOUTUBE_API_KEY: ${YOUTUBE_API_KEY:-}
    restart: unless-stopped

volumes:
  yt_data:
