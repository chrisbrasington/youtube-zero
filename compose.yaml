services:
  youtube-zero:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data  # database persisted to ./data/youtube_zero.db on the host
    environment:
      DB_PATH: /data/youtube_zero.db
      YOUTUBE_API_KEY: ${YOUTUBE_API_KEY:-}
    restart: unless-stopped
